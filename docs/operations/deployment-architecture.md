---
title: 部署架构
sidebar_position: 3
---

# 部署架构

部署前先把平台、基础依赖和业务应用分成三个故障域。

| 层次 | 组成 | 独立维护的内容 |
| --- | --- | --- |
| 平台层 | `ouroboros-develop`、开发工作区、平台配置 | 平台版本、登录、模型管理、应用版本和运行控制 |
| 依赖层 | MySQL、RabbitMQ、Docker、文件存储 | 持久化、账号、容量、备份和监控 |
| 应用层 | 业务应用运行实例及其数据 | 应用版本、基座镜像、业务数据库和验收结果 |

平台升级、业务应用发布和依赖升级分别执行，不得共用一个回退判断。

## 选择环境形态

| 环境 | 适用场景 | 最低要求 |
| --- | --- | --- |
| 本地体验 | 个人学习、功能验证 | 单机 Compose；数据允许重建 |
| 团队共享 | 开发、测试、联调和培训 | 固定地址、持久化存储、共享账号管理、定期备份 |
| 长期运行 | 正式或类生产业务 | 独立依赖、变更窗口、监控告警、恢复演练、制品与凭据管理 |

本地体验环境可以把三个层次部署在同一台主机；团队共享和长期运行环境仍要保持数据目录、凭据、版本记录和故障责任分离。

## 最小 Compose 拓扑

手册提供的 Compose 环境包含：

```text
用户 / 反向代理
        |
ouroboros-develop ---- Docker Socket ---- 业务应用实例
        |                    |
        +---- MySQL          +---- 应用数据库
        +---- RabbitMQ
        +---- workspace / logs / file storage
```

正式使用前确认：

- `ouroboros-develop` 与 `ouroboros-mothership` 使用配套版本。
- MySQL、RabbitMQ、工作区、日志、上传目录和文档库目录位于持久化存储。
- `HOST_WORKSPACE_PATH` 指向宿主机真实工作区，容器内 `/app/deploy` 挂载与之对应。
- Docker Socket 仅对需要创建运行实例的平台容器开放。
- 外部入口、TLS、端口和防火墙规则已经记录。
- 平台管理员、数据库管理员、应用发布人和回退决策人已经明确。

## 发布边界

| 变更 | 操作入口 | 不得同时假设发生的事 |
| --- | --- | --- |
| 发布应用版本 | 应用版本管理 | 不会自动更新运行实例 |
| 更新运行实例镜像 | 运行实例 | 不会切换应用元数据 tag |
| 升级开发平台 | 平台 Compose 和 `.env` | 不会发布业务应用 |
| 升级 MySQL 或 RabbitMQ | 依赖组件流程 | 不属于平台镜像升级 |

应用发布见 [业务应用发布与回退](./application-release-and-rollback)，平台镜像变更见 [平台升级与回滚](./release-upgrade-and-rollback)。

## 进入安装前的完成标准

- 环境类型和三层部署位置已经确定。
- 所有持久化目录、备份目标和容量责任人已经确定。
- 域名、端口、代理、TLS 和依赖网络可达性已经确认。
- 应用发布、平台升级和依赖升级使用独立变更记录。

满足后继续执行 [环境要求](./environment-requirements) 和 [安装与初始化](./installation-and-initialization)。
