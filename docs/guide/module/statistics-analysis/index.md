---
sidebar_position: 1
---

# 统计分析

统计分析的开发态链路为：

```text
应用数据源 -> 数据集 / Dataset Studio -> 图表 / Chart Editor
-> 仪表板 / Dashboard Composer -> 运行预览
```

适用于统一指标口径、展示趋势与分布、复用查询参数，以及把多张图表组合成角色看板。仅维护业务记录时使用业务模型列表页；需要自动写数据或调用接口时使用逻辑流。

## 前置条件

- 管理员已创建平台数据源，应用开发者已创建并绑定 [应用数据源](../../../low-code/development-platform-menus/datasource)。
- 已确定查询口径、数据范围、参数、查看角色和一组可核对的样本结果。
- 查询账号只拥有所需数据权限；SQL 不包含写入或结构变更操作。

## 创建数据集

1. 新建数据集，填写名称、标题和应用数据源。
2. 填写查询 SQL。
3. 定义参数；在 Dataset Studio 中配置测试参数和测试数据源。
4. 点击 **执行并推导字段**，检查返回结果和字段 Schema。
5. 为每个字段核对字段名、标题、数据类型和语义角色。
6. 按需要配置字段排序、筛选、导出和默认展示。
7. 保存草稿，执行预览并与已知样本核对。
8. 执行发布检查，修复缺失的数据源、参数、字段或查询错误。

成功标志：相同参数得到稳定结果，字段类型与语义正确，预览值与已知样本一致。

:::warning 当前发布边界
新建数据集状态为 `DRAFT`。当前界面提供保存草稿、执行、推导字段、预览和发布检查，但没有把数据集切换为 `PUBLISHED` 的独立发布按钮；运行时 Provider 只加载 `PUBLISHED` 数据集。因此，本页步骤完成的是开发态创建和预览，不代表数据集已经可供正式运行时加载。正式交付前，必须按当前部署环境提供的发布流程确认数据集已进入 `PUBLISHED` 状态；没有该流程时，不要把预览结果当作正式发布结果。
:::

## 创建图表

1. 新建图表并选择已验证的数据集。
2. 选择图表类型：指标卡、折线图、柱状图、饼图、面积图、散点图、交叉表或明细表。
3. 在 Chart Editor 中配置维度、指标、系列、排序和显示格式。
4. 分别使用正常数据、空结果和边界参数预览。
5. 保存后重新打开，确认图表仍引用正确的数据集和字段。

| 分析目标 | 推荐图表 |
| --- | --- |
| 总量、完成率等单值 | 指标卡 |
| 时间趋势 | 折线图或面积图 |
| 类别、部门、时间段对比 | 柱状图 |
| 占比 | 饼图；类别过多时改用柱状图 |
| 两个数值变量的关系 | 散点图 |
| 多维汇总 | 交叉表 |
| 明细查询、筛选和导出 | 明细表 |

成功标志：图表回答一个明确问题；空结果不报错；筛选和排序后的数据与数据集预览一致。

## 组装仪表板

1. 新建仪表板，进入 Dashboard Composer。
2. 设置栅格列数和行高。
3. 添加图表，填写组件 ID 和标题。
4. 拖拽图表并调整宽高，保证常用桌面分辨率下无需横向滚动。
5. 打开运行预览，验证每张图表的加载、位置、尺寸和筛选结果。
6. 使用有权限和无权限账号分别验证页面及数据访问。

成功标志：图表互不遮挡，刷新后布局稳定，有权限用户看到正确数据，无权限用户无法取得页面或受限数据。

## 常见问题

### 执行 SQL 后没有推导出字段

先检查应用数据源绑定、测试参数和 SQL 是否能够返回结果。参数为空时不应执行的查询，要为测试参数填写有效值。

### 开发态预览正常，运行时找不到数据集

检查数据集状态。运行时只加载 `PUBLISHED`；`DRAFT` 只能用于开发态编辑和预览。

### 图表结果与数据集不一致

核对图表使用的数据集版本、维度和指标字段、聚合方式、排序以及筛选参数。先在数据集预览复现，再检查图表配置。

### 仪表板显示拥挤或重叠

统一栅格列数和行高，重新调整每个组件的位置与尺寸；不要用多个图表占用同一栅格区域。
